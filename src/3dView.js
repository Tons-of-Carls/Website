var scene=new THREE.Scene();var camera=new THREE.PerspectiveCamera(75,2,0.1,1000);var renderer=new THREE.WebGLRenderer();renderer.setClearColor(0xfff,1);renderer.setSize(window.innerWidth,window.innerHeight);renderer.domElement.style.padding = "0px";renderer.domElement.style.margin = "0px";document.getElementById("canvas").appendChild(renderer.domElement);var controls=new THREE.OrbitControls(camera,renderer.domElement);camera.position.z=5;var loader=new THREE.PLYLoader();loader.load('./files/model.ply',function(geometry){geometry.computeVertexNormals();console.log("generating...",geometry)
  var material=new THREE.MeshBasicMaterial({shininess:1000,vertexColors:THREE.FaceColors});var mesh=new THREE.Mesh(geometry,material);mesh.position.y=-1;mesh.position.z=-1;mesh.position.x=-1;mesh.scale.multiplyScalar(0.1);mesh.castShadow=true;mesh.receiveShadow=true;scene.add(mesh);});function animate()
{requestAnimationFrame(animate);renderer.render(scene,camera);}
animate();document.body.style.margin = "0px";